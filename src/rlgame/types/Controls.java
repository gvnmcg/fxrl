package rlgame.types;

public enum Controls {

    MOVE, LOOK,
    UP, DOWN, LEFT, RIGHT,

    UI, OPEN, CLOSE,
    PAUSE,
    INV, BODY, SKILL,

    TOGGLE,
    HABIT,
    VIEW,
    OVERLAY,

    FOCUS
}
