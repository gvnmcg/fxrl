package rlgame;

import engine.AbstractGame;
import engine.InputHandler;
import javafx.scene.Group;

public class RLInputHandler extends InputHandler {


    public RLInputHandler(Group root, AbstractGame game) {
        super(root, game);
    }
}
