package engine.propeties;

import javafx.scene.image.Image;

public class GameObjectAnimation {

    //load set of images into an array
    //num frames is array length
    // every frame incease frame counter (float by 'speed')

    private Image[] frames;

    Image animationFrame;

    private int frameCount = 0;

}
